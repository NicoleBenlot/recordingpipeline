# Core audio dependencies
sounddevice>=0.4.6
numpy>=1.24.0

# Spectral-gate noise reduction
noisereduce>=3.0.0

# Audio codec / MP3 encoding (requires ffmpeg on PATH)
pydub>=0.25.1

# Environment / configuration loading (.env)
python-dotenv>=1.0.0
